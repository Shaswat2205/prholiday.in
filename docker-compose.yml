version: '3.9'
services:
  # ── Monolith Backend ──────────────────────────────────────────────
  server:
    build: ./server
    ports: ['5000:5000']
    depends_on: [mongo]
    environment:
      MONGODB_URI: mongodb+srv://shaswat2205_db_user:WONUID7CsK9arU3F@sahuvault.iavqh4q.mongodb.net/
      JWT_SECRET: prholidays_secret_key_2024
      PORT: 5000
      NODE_ENV: development
      CLIENT_URL: http://localhost:5173

  # ── Databases ─────────────────────────────────────────────────────
  mongo:
    image: 'mongo:7'
    ports: ['27017:27017']

  redis:
    image: 'redis:7-alpine'
    ports: ['6379:6379']
